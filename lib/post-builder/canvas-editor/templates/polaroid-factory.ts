/**
 * 2026-05-24 — ORPHAN FACTORY (no longer imported).
 *
 * This file used to export a factory function that generated 15 canvas
 * templates. The full 90-template factory catalog was deleted on
 * 2026-05-24 in favor of the 10 minimal placeholders in
 * `placeholder-factory.ts`. The file is kept on disk only because the
 * sandbox we used to do the refactor cannot delete files; the user is
 * expected to delete this file via GitHub Desktop when pushing.
 *
 * If you are reading this in a normal session and the file still exists,
 * delete it — nothing imports from it.
 */
export {};
