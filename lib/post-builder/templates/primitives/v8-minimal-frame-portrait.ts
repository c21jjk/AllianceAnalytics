/**
 * 2026-05-24 — ORPHAN PRIMITIVE (no longer imported).
 *
 * This file used to render a single (variant × format) combination as an
 * HTML React component for the legacy Chromium-HTML render path. The full
 * V1 primitives catalog was deleted on 2026-05-24 in favor of routing all
 * renders through the canvas-editor schema pipeline. The file is kept on
 * disk because the sandbox we used to do the refactor cannot delete files;
 * the user is expected to delete this file via GitHub Desktop when pushing.
 */
export {};
