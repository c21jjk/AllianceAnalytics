/**
 * 2026-05-24 — ORPHAN FILE (no longer imported).
 *
 * This file used to export `POST_TYPE_THEMES` for the V1 HTML primitives.
 * The V1 primitives were deleted on 2026-05-24 and no live consumer
 * remains. The minimal shim in `primitives/_shared.ts` exports the
 * `PostTypeTheme` type for type-only back-compat; this file's runtime
 * exports are no longer needed.
 *
 * Kept on disk because the sandbox cannot delete files; the user is
 * expected to delete it via GitHub Desktop when pushing.
 */
export {};
