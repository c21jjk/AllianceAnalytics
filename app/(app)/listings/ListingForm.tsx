/**
 * Deprecated shim: ListingForm was renamed to PropertyForm and lives under
 * /components. This file re-exports it so any straggler imports keep
 * compiling. Remove once we've confirmed no remaining importers.
 */
export { default } from "@/components/PropertyForm";
