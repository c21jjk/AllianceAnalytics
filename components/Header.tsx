/**
 * Deprecated: the global Header was folded into <TopNav /> when the sidebar
 * was retired in favor of a horizontal top nav. The TopNav now renders the
 * brand mark, the primary tabs, AND the AccountMenu in a single bar — so a
 * separate Header is no longer needed.
 *
 * This stub remains so any stale import resolves and typechecks; the
 * component renders nothing.
 */
export default function Header() {
  return null;
}
